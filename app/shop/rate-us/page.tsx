import RateUsForm from "@/components/forms/platform-rate-form";

export default function Page() {

    return (
        <main className="p-12 flex items-center justify-center min-h-screen ">
            <RateUsForm />
        </main>
    );
}